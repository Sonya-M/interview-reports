import React from "react";
import { propTypes } from "react-bootstrap/esm/Image";
import styles from "./NDCard.module.css";

export default function NDCard(props) {
  return (
    <div className="">
{props.children}
    </div>
  )
}